---
- name: Set instanceType as cloud
  ini_file:
    dest: "{{ splunk.home }}/etc/system/local/server.conf"
    section: general
    option: "instanceType"
    value: "cloud"
    owner: "{{ splunk.user }}"
    group: "{{ splunk.group }}"
  become: true
  become_user: "{{ splunk.user }}"
  